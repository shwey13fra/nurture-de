"""Vendored cl100k_base encoder + vocab (moved in-repo 2026-08-06, PM-5).

Was imported from a prior session's Temp scratchpad via a hardcoded path, so the
tracked chunks.jsonl was not actually reproducible. Now self-contained.
"""
